name: remote ssh command

on: [push]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Check network connectivity
      run: |
        ping -c 4 127.0.0.1
        nc -zv 127.0.0.1:4040
